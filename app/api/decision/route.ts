import { NextResponse } from "next/server";
import { generateDebrief } from "../../../lib/ai/client";
import { getProgress, getSupabaseServerClient, recordAttempt, upsertProgress } from "../../../lib/db";
import { getLevelBySlug } from "../../../lib/game/content";
import { getAnonymousSessionUserId } from "../../../lib/session";
export const runtime = "nodejs";
export async function POST(request: Request) { try { const userId=await getAnonymousSessionUserId(); const body=await request.json() as {slug?:string;nodeId?:string;decisionId?:string}; const level=body.slug?getLevelBySlug(body.slug):undefined; if(!userId||!level||level.gameplay.kind!=="narrative"||!body.nodeId||!body.decisionId)return NextResponse.json({error:"Invalid decision"},{status:400}); const node=level.gameplay.nodes.find(n=>n.id===body.nodeId);const decision=node?.decisions.find(d=>d.id===body.decisionId);if(!node||!decision)return NextResponse.json({error:"Missing decision"},{status:400});const {data}=await getSupabaseServerClient().from("attempts").select("payload").eq("user_id",userId).eq("level_id",level.id).contains("payload",{kind:"narrative_state"}).order("created_at",{ascending:false}).limit(1).maybeSingle();const previous=data?.payload as {metrics?:Record<string,number>}|null;const before=previous?.metrics??Object.fromEntries(level.gameplay.metrics.map(m=>[m.id,m.start]));const metrics={...before};for(const [id,delta] of Object.entries(decision.metricDeltas))metrics[id]=(metrics[id]??0)+delta;const debrief=await generateDebrief({userId,levelId:level.id,levelTopic:level.topic,node,decision,metricDefs:level.gameplay.metrics,metricsBefore:before,metricsAfter:metrics});const completed=decision.nextNodeId===null;await recordAttempt({userId,levelId:level.id,payload:{kind:"narrative_state",currentNodeId:decision.nextNodeId,metrics,completed},score:debrief});const was=(await getProgress(userId)).some(p=>p.level_id===level.id&&p.status==="completed");const awardedXp=completed&&!was?level.xpReward:0;if(completed)await upsertProgress({userId,levelId:level.id,status:"completed",xpEarned:awardedXp});return NextResponse.json({debrief,state:{currentNodeId:decision.nextNodeId,metrics},completed,awardedXp}); } catch { return NextResponse.json({error:"Could not save that decision."},{status:500}); } }
